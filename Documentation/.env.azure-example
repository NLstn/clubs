# Azure Container Apps Environment Variables Example
# Copy this file and update the values for your deployment

# Database Configuration (Required)
DATABASE_URL=your-postgres-server.postgres.database.azure.com
DATABASE_PORT=5432
DATABASE_USER=clubs
DATABASE_USER_PASSWORD=your-secure-password

# Azure AD Configuration (Required)
AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-client-id  
AZURE_CLIENT_SECRET=your-client-secret

# Frontend Configuration (Optional)
FRONTEND_URL=https://your-frontend-url.com

# Azure Communication Services (Optional - for email functionality)
AZURE_ACS_ENDPOINT=https://your-acs-resource.communication.azure.com
AZURE_ACS_SENDER_ADDRESS=donotreply@your-domain.com

# Notes:
# - Use Azure Container Apps secrets for sensitive values like passwords and secrets
# - Database should be accessible from Azure Container Apps environment
# - Frontend URL should match your actual frontend deployment for CORS
# - ACS configuration is optional but required for email notifications